volumes:
  craft-api_postgres_todos_data:
    driver: local

services:
  postgres_todos_db:
    build:
      context: ./postgresql
    env_file:
      - ./postgresql/env
    ports:
      - 5432:5432
    volumes:
      - craft-api_postgres_todos_data:/var/lib/postgresql/data
